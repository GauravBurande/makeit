import { getUser } from "@/lib/server-db-calls";

const Studio = async () => {
  //   const user = await getUser();

  //   console.log(user);
  return (
    <main>
      <h2>Studio</h2>
    </main>
  );
};

export default Studio;
